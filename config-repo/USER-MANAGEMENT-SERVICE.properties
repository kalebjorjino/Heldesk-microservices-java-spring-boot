# Configuraci�n para el User Management Service
server.port=8083

# Lee la clave secreta desde una variable de entorno llamada JWT_SECRET
app.jwt.secret=${JWT_SECRET}

# Tiempo de expiraci�n del token (86400000 = 1 d�a)
app.jwt.expiration-ms=86400000

# Conexi�n a Eureka
eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka

# Forzar hostname manualmente
spring.cloud.client.hostname=user-management-service

# Evitar que use la IP detectada autom�ticamente
eureka.instance.prefer-ip-address=false

# Forzar hostname manualmente (IMPORTANTE)
eureka.instance.hostname=user-management-service

# Conexi�n a la Base de Datos
spring.datasource.url=jdbc:mysql://mysql-db:3306/helpdesk_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=3Nf4rm1t3c1root5
spring.jpa.hibernate.ddl-auto=update


