# Configuraci�n para el Helpdesk Ticket Service
server.port=8082

# Conexi�n a Eureka
eureka.client.serviceUrl.defaultZone=http://eureka-server:8761/eureka

# Forzar hostname manualmente
spring.cloud.client.hostname=helpdesk-ticket-service

# Evitar que use la IP detectada automticamente
eureka.instance.prefer-ip-address=false

# Forzar hostname manualmente (IMPORTANTE)
eureka.instance.hostname=helpdesk-ticket-service

# Conexin a la Base de Datos
spring.datasource.url=jdbc:mysql://mysql-db:3306/helpdesk_db?useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=3Nf4rm1t3c1root5
spring.jpa.hibernate.ddl-auto=update
